extends base
include mixins/video

block uploadLink 
    nav 
        ul 
            li 
                a(href="videos/upload") Upload Video

block content
    h2 Welcome here you will see the trending videosul 
    each item in videos 
        +video(item)
    else
        li Sorry nothing found.